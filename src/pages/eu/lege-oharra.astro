---
import Base from '@layouts/Base.astro';
import LegalPage from '@components/LegalPage.astro';
import { useTranslations } from '@i18n/index';
import { avisoLegal, legalLastUpdate } from '@content/legal';

const lang = 'eu' as const;
const t = useTranslations(lang);
const data = avisoLegal.eu;
---

<Base
  lang={lang}
  currentPath="/aviso-legal"
  pageTitle={data.title}
  pageDescription="Erreka Meet Point-en lege oharra. Datu identifikatzaileak, erabilera baldintzak eta jabetza intelektuala."
>
  <LegalPage
    label={t('legal.label.info')}
    title={data.title}
    sections={data.sections}
    lastUpdate={legalLastUpdate}
    lastUpdateLabel={t('legal.last_update')}
  />
</Base>
